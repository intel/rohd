name: rohd_source_navigator
description: >
  Dart implementation of the ROHD source navigator — path normalisation,
  frame cycling, and DTD service for cross-probe source navigation.
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  json_rpc_2: ^3.0.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  test: ^1.24.0
