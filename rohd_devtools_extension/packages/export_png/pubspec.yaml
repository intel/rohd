name: export_png
description: "Platform-conditional PNG export — shared across ROHD DevTools sub-packages."
homepage: https://intel.github.io/rohd-website/
version: 0.1.0

publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  web: ^1.0.0

dev_dependencies:
  lints: ^5.0.0
